# 插件运行时配置（Runner 自动生成，可能含 access_token 等凭据）——不入库
/config.toml

# 插件运行期落盘状态（会话元数据等，随运行环境变化）——不入库
/data/
/runtime/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 测试
.pytest_cache/
.coverage
htmlcov/
